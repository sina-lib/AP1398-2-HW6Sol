dist: bionic
sudo: false

language: cpp
compiler: gcc

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8

install: make

script: make  sina-lib/AP1398-2-HW6Sol  &&  sina-lib /AP1398-2-HW6Sol/app

